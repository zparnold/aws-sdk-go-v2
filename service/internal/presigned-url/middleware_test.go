package presignedurl

import "testing"

func TestPresignMiddleware(t *testing.T) {
	cases := map[string]struct {
	}{}

	for name, c := range cases {
		t.Run(name, func(t *testing.T) {
			t.Errorf("not implemented, %v", c)
		})
	}
}
